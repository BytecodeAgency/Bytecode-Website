language: node_js

node_js:
  - "stable"
  - "node"
  - "lts/*"
  - "9"
  - "8"

notifications:
  email:
    receipients:
      - info@bytecode.nl

# before_install:

install:
  - yarn
  - yarn run export

# before_script:

script:
  - yarn run start

cache:
  yarn: true
  directories:
    - node_modules
