{
    "name": "@bytecode/monorepo",
    "private": true,
    "workspaces": ["website", "components"],
    "scripts": {
        "dev:components": "cd components && yarn dev",
        "dev:website": "cd website && yarn dev",
        "dev": "yarn dev:components && yarn dev:website"
    }
}