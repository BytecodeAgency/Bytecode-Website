image: node:8-alpine

cache:
  paths:
  - node_modules/

before_script:
  - npm i -g yarn
  - npm i # To fix Yarn error
  - cp .env.example .env

build:
  script:
    - yarn run lint
    - yarn run build
    - yarn run export
